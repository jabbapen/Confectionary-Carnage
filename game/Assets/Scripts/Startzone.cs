using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Startzone : MonoBehaviour
{
    protected static Startzone Instance = null;

    private void Awake()
    {
        //if (Instance != null)
        //{
        //    Destroy(Instance.gameObject);
        //}
        //Instance = this;
    }
}
